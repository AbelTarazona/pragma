name: pragma_challenge
description: A Very Good Project created by Very Good CLI.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  bloc: ^8.1.1
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.2
  flutter_localizations:
    sdk: flutter
  intl: ^0.17.0
  cat_repository:
    path: packages/cat_repository
  cat_api:
    path: packages/cat_api
  flutter_dotenv: ^5.0.2
  json_annotation: ^4.8.0
  equatable: ^2.0.5
  cached_network_image: ^3.2.3
  searchable_listview: ^2.3.2

dev_dependencies:
  bloc_test: ^9.1.2
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0
  very_good_analysis: ^3.1.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/.env
